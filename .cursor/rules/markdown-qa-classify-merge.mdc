---
description: @markdown-qa-classify-merge 直写 study/interview；答为总结、按内容多分类、无 bak
alwaysApply: false
---

# Markdown 问答分类与合并

用户 **@markdown-qa-classify-merge** 并附带 `.md` 时：

1. **直写**，不预览、不确认。
2. **答**：总结撰写，禁止复制参考答案/评价。
3. **分类**：按每条语义写入多个 `{分类名}问答.md`，禁止整份单文件笼统归类。
4. **禁止** `.bak.md`；只保留最终分类文件与 `_merge_log.md`。

细则见 `.cursor/agents/markdown-qa-classify-merge.md`。
