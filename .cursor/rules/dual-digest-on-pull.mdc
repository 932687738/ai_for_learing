---
description: 用户说「拉取」时同时执行 AI 日报、知识库摘要与掘金热榜三个 skill（非 Git 语境）
alwaysApply: true
---

# 三 Digest：「拉取」统一触发

当用户在本仓库中的消息**主要意图**是拉取/更新**日报类汇总**（例如：`拉取`、`拉取一下`、`帮我拉取`、`拉取日报`、`拉取 AI 和知识库` 等），且**不是** Git / 版本控制场景（未以 `git pull`、合并远程分支、`remote`/`rebase` 等为主意图）时：

1. **必须先 Read** 以下三个文件全文，并**严格按其步骤执行**（不得只执行其中一部分）：
   - `.cursor/skills/ai-daily-digest/SKILL.md`
   - `.cursor/skills/knowledge-base-digest/SKILL.md`
   - `.cursor/skills/juejin-hot-digest/SKILL.md`
2. **同一轮对话内顺序完成三件事**：完成 **AI Daily News Digest**（`ai-daily-digest`）规定流程 → 再完成 **Knowledge Base Digest**（`knowledge-base-digest`）规定流程 → 再完成 **Juejin Hot Digest**（`juejin-hot-digest`）规定流程（若检索量大，可先并行收集再分三节写入；掘金热榜必须以 `python tools/juejin_hot_fetch.py` 抓取列表与正文后再归纳）。
3. **日期与 force**：用户未写明 `force=true`、也未带 `1`/`2`/具体日时，三个 skill 都要求先选方式——**只向用户提问一次**：「请选择拉取方式：1 增量 … 2 指定 YYYY-MM-DD …（可选 force=true）」；用户回复后 **三个 skill 共用同一选择**（同一 `force`、同一增量区间或同一指定日）。对掘金热榜：方式 `1`/`2`/`force` 的语义以 `juejin-hot-digest` SKILL 为准（快照日/章节日 + `seen_urls` 去重），不得另开一套日期问答。
4. **收尾说明**须分别交代：
   ① AI 日报处理日期与写入路径（**当月归档 `dailyReport/ai-daily-news/YYYYMM.md` + 滚动入口 `ai-daily-digest.md`** + 状态文件）；
   ② 知识库摘要处理日期与写入路径（**当月归档 `dailyReport/knowledge-base-news/YYYYMM.md` + 滚动入口 `knowledge-base-digest.md`** + 状态文件），并**包含** `dailyReport/github-topz.md`（由 `python tools/update_github_topz.py` 在本次知识库拉取内更新，与 `knowledge-base-digest.md` 同批）。`github-topz.md` 中 **全局 Star 表**与 **各 Trending 子表**均含 **「标记」列**：相对**本次拉取前**文件中该表已出现过的 `owner/repo`，本轮首次出现的行标 **新增**；**下一次拉取**会清空上一轮「新增」后按同一规则重算（仅标记相对**上一版磁盘文件**的新增行）；
   ③ 掘金热榜快照日期与写入路径（**当月归档 `dailyReport/juejin-hot-news/YYYYMM.md` + 滚动入口 `dailyReport/juejin-hot-news/juejin-hot-digest.md`** + `juejin-hot-state.json`），并交代本轮列表条数、新 URL 数、跳过已见数、详情失败数（路径以各 SKILL「固定路径」为准，当前在项目 `dailyReport/` 下）。
5. 若用户**仅**在搞 Git（例如明确 `git pull`、同步分支），**忽略**本条，不按 digest 流程执行。
6. 若用户**明确只拉其中一路**（例如仅 `拉取AI新闻`、仅 `拉取知识库`、仅 `拉取掘金热榜`），则只执行对应单个 skill，**不要**因本条强行跑满三路。
